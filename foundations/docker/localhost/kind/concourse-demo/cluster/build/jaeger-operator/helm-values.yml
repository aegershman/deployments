---
jaeger:
  create: true
  spec:
    strategy: allInOne
    allInOne:
      options:
        log-level: debug
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: nginx
      hosts:
        - jaeger.vcap.me

---
# resources:
#   - name: time
#     type: time
#     source:
#       interval: 30s

jobs:
  - name: make-a-change
    plan:
      # - get: time
      #   trigger: true
      - task: create-new-uuid
        config:
          platform: linux
          image_resource:
            type: docker-image
            source:
              repository: czero/cflinuxfs2
          run:
            path: bash
            args:
              - -c
              - |
                #!/usr/bin/env bash

                set -eu
                echo $(date)

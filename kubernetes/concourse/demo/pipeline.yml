---
# resources:
#   - name: time
#     type: time
#     source:
#       interval: 30s

jobs:
  - name: make-a-change
    plan:
      # - get: time
      #   trigger: true
      - task: create-new-uuid
        config:
          platform: linux
          image_resource:
            type: docker-image
            source:
              repository: alpine:3.9
          run:
            path: bash
            args:
              - -c
              - |
                #!/usr/bin/env bash

                set -eu
                echo $(date)
